import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bunkr.smartalbumdownloader',
  appName: 'Bunkr Smart Album Downloader',
  webDir: 'dist'
};

export default config;
